# 🛠 Дневник Разработки: Хроники Цитадели (2025-2026)

Этот документ — стратегическая шпаргалка и технический лог проекта **Analog Mission**. Здесь зафиксированы ключевые решения, архитектурные приемы и методы борьбы с "цифровыми демонами" (багами).

## 🏛 1. Архитектурная Суть
Проект построен на **Analog.js** (Fullstack Angular Meta-framework). 
- **Маршрутизация:** File-based routing (папка `src/app/pages`).
- **Контент:** Markdown-файлы (`.md`) в папке `src/content`.
- **Рендеринг:** SSR (Server Side Rendering) с последующей гидратацией.

---

## 🎨 2. Дизайн и Стилизация (The Mystic Dark Theme)
Мы внедрили уникальную палитру **"Изумрудная Цитадель"**:
- **Цвета:** - Фон: `#020617` (Slate 950).
  - Акценты: `Emerald-500` (#10b981) и `Cyan-400`.
  - Текст: `Slate-200` / `Zinc-300`.
- **Типография:** Сочетание `font-sans` для интерфейса и `font-serif` (иногда с курсивом) для глубоких смыслов в статьях.
- **Эффекты:** - Градиентная анимация заголовков (`animate-gradient-portal`).
  - Стеклянный эффект (`backdrop-blur`) для навигации.

---

## ⚙️ 3. Ключевые Технические Решения

### Динамические Страницы Постов (`[slug].page.ts`)
Мы отказались от жесткой привязки к сервисам в пользу нативного `injectContent<PostAttributes>('slug')`. 
- **Важно:** Это позволяет Analog автоматически сопоставлять URL с именем файла в `/content/blog/`.

### Система Тегов (`/tags/[tag].page.ts`)
Реализована фильтрация через `injectContentFiles`. 
- **Фильтр:** `file => file.filename.endsWith('.md')` — это критически важно, чтобы Vite не захлебнулся при индексации файлов.
- **Слаживание:** Теги в списке статей — это активные ссылки `routerLink`, ведущие на страницу архива по ключу.

### Решение проблем с Video/Audio (Next.js Legacy)
*Для справки, если будем переносить части со старого TailwindBlog:*
- Проблема CSP (Content Security Policy) решалась добавлением `https://www.youtube.com` в `frame-src`.
- React-атрибуты требуют CamelCase: `frameBorder` вместо `frameborder`.

---

## 🧹 4. Протокол Очистки (Борьба с Vite)
Если изменения в коде (особенно в стилях или тексте ссылок) не отображаются в браузере — это **Cache Lock**.
**Лекарство:**
```bash
# Полная очистка
rm -rf node_modules/.vite .analog/cache
# Принудительный запуск
npm run dev -- --force
